const { firestore } = require('firebase-admin');

module.exports = class WithdrawCotroller{
    
    static async doWithdraw(){
        //check current balance
        //create pending trans
        //subs amount from balance
    }
}